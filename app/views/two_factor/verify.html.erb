<h1>Two-Factor Authentication</h1>

<p>Enter the 6-digit code from your authenticator app, or a backup code.</p>

<%= form_with url: two_factor_verify_path, method: :post do |form| %>
  <div>
    <%= form.label :otp_code, "Verification code" %>
    <%= form.text_field :otp_code, maxlength: 10, autocomplete: "one-time-code", required: true %>
  </div>
  <div>
    <%= form.submit "Verify" %>
  </div>
<% end %>
